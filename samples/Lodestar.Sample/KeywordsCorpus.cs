namespace Lodestar.Sample;

/// <summary>The document every keyword-extraction sample scores.</summary>
/// <remarks>
/// A helper, not a sample: it names no public type of its own. Four files quoting the
/// same sentence is duplication only SonarCloud can see, and a reader reads as four
/// different documents.
/// </remarks>
internal static class KeywordsCorpus
{
    public const string Document =
        "Compatibility of systems of linear constraints over the set of natural numbers. "
        + "Criteria of compatibility of a system of linear Diophantine equations.";
}
