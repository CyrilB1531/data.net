# DEVELOPMENT-only dependencies (oracle generation).
# Not part of the distributed package. Permissive licenses (MIT / BSD / Apache).
# python-Levenshtein (GPL) is deliberately excluded — see docs/decisions/0003.
#
# Versions are pinned exactly, not ranged. These libraries are not merely build
# inputs: the committed corpora under tests/oracles/ ARE their output, and CI
# regenerates and diffs them on every pull request. Under a range, a silent
# minor release would land as an unexplained oracle diff on an unrelated change.
# Bump deliberately, and regenerate the corpora in the same commit.
#
# Install with --only-binary :all: so that no source distribution runs setup.py.
rapidfuzz==3.14.5
jellyfish==1.2.1
textdistance==4.6.3
scikit-learn==1.9.0
nltk==3.10.1
tokenizers==0.23.1
numpy==2.5.1
sentencepiece==0.2.2
# sentencepiece_model_pb2 — the generated protobuf module the spiece.model and
# tokenizer.json generators read, and that fetch_xlmr_vocab.py writes the XLM-R
# fixture with — imports google.protobuf, which sentencepiece
# does not declare as a dependency. Without it here the lock omits it and CI fails
# with "No module named 'google'" while a developer machine that happens to have
# protobuf installed passes.
protobuf==7.35.1
# onnx is only needed to (re)build the tiny synthetic test model, not for
# generate_oracles.py: onnx==1.16.0

# pytest — the only test runner in tools/. tools/generate_sonar_globalconfig.py
# parses two foreign formats and writes a file the build reads; a drift check
# proves that generator stable, never that it is right.
pytest==8.4.2
