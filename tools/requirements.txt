# Dependencies for DEVELOPMENT only (oracle generation).
# Not part of the distributed package. Permissive licenses (MIT / BSD / Apache).
# python-Levenshtein (GPL) is deliberately excluded — see docs/decisions/0003.
rapidfuzz>=3.14,<4
jellyfish>=1.2,<2
textdistance>=4.6,<5
scikit-learn>=1.9,<2
nltk>=3.9,<4
tokenizers>=0.23,<1
numpy>=1.26
# onnx is only needed to (re)build the tiny synthetic test model, not for
# generate_oracles.py: onnx>=1.16
